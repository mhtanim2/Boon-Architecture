# interfaces-database

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build interfaces-database` to build the library.

## Running unit tests

Run `nx test interfaces-database` to execute the unit tests via [Jest](https://jestjs.io).
