# interfaces-boon-api

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build interfaces-boon-api` to build the library.

## Running unit tests

Run `nx test interfaces-boon-api` to execute the unit tests via [Jest](https://jestjs.io).
